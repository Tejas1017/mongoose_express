<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit book</title>
</head>
<body>  
    <h1>Edit <%= book.name %> here:</h1>
    <form action="/books/<%= book._id %>?_method=PUT" method="POST">
        <label for="name">Book name</label>
        <input type="text" name="name" id="name" placeholder="book name" value="<%= book.name %>">
        <label for="price">Book price</label>
        <input type="number" name="price" id="price" placeholder="book price" value="<%= book.price %>">
        <label for="author">Book author name</label>
        <input type="text" name="author" id="author" placeholder="book author" value="<%= book.author  %>">
        <label for="category">Book category</label>
        <select name="category" id="category">
            <option value="History" <%= book.category ==='History'?'selected':"" %>>History</option>
            <option value="Self Growth" <%= book.category ==='Self Growth'?'selected':"" %>>Self Growth</option>
            <option value="Novel" <%= book.category ==='Novel'?'selected':"" %>>Novel</option>
        </select>
        <button >
            Save
        </button>
    </form>
    <a href="/books/<%= book._id %>">cancel</a>
</body>
</html>